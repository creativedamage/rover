{% extends "index" %}

{% block content %}
    <main> 
       <!-- Hero Area Start -->
       <div class="slider-area2 slider-height2 d-flex align-items-center">
        <div class="container">
            <div class="row">
                <div class="col-xl-12">
                    <div class="hero-cap text-center pt-50">
                        <h2 class="text-white">About Us</h2>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Hero Area End -->
    <!-- About Details Start -->
    <div class="about-details section-padding30">
        <div class="container">
            <div class="row">
                <div class="offset-xl-1 col-lg-8">
                    <div class="about-details-cap mb-50">
                        <h4>{{entry.missionTitle}}</h4>
                        <p>{{entry.missionText}}</p>
                    </div>

                    <div class="about-details-cap mb-50">
                        <h4>{{entry.aboutTitle}}</h4>
                        <div>{{entry.aboutText}}</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- About Details End -->
    

    </main>
{% endblock %}